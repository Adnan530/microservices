server:
  port: 8084

spring:
  application:
    name: Config-Server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Adnan530/microservice-configuration
          clone-on-start: true
